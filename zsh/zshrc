# Path to your oh-my-zsh configuration.
ZSH=$HOME/.oh-my-zsh

ZSH_THEME="sunrise"

# Which plugins would you like to load? (plugins can be found in ~/.oh-my-zsh/plugins/*)
# Custom plugins may be added to ~/.oh-my-zsh/custom/plugins/
# Example format: plugins=(rails git textmate ruby lighthouse)
plugins=(git autojump virtualenvwrapper)

source $ZSH/oh-my-zsh.sh

# Tells virtualwrapper where is my virtualenv folder
export WORKON_HOME=~/.virtualenvs/

# Customize to your needs...
export PATH=/usr/lib/colorgcc/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/usr/games

### Added by the Heroku Toolbelt
export PATH="/usr/local/heroku/bin:$PATH"

# Aliases

alias djangoshell="python manage.py shell"
alias shellplus="python manage.py shell_plus --use-pythonrc"
alias jb=jobs

function git.send {
  git push origin `git rev-parse --abbrev-ref HEAD` $1
}
